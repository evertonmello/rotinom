import { UltimoPingPipe } from './ultimo-ping.pipe';

describe('UltimoPingPipe', () => {
  it('create an instance', () => {
    const pipe = new UltimoPingPipe();
    expect(pipe).toBeTruthy();
  });
});
